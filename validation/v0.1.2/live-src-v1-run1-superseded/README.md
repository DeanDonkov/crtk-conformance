# live-src-v1-run1-superseded — live run at commit f9e2b51, superseded

Live run of crtk-conformance 0.1.2 at the first freeze (commit f9e2b51) against SRC v1 on AMBF, taken directly
after mock-run1-superseded. The code was re-frozen for the rate estimator change described in
`../mock-run1-superseded/README.md` (a change that cannot affect these runs, since the SRC publishes no
`setpoint_cp`; the only other change is the wording of the liveness finding string) and the live runs were
repeated at the re-frozen commit so that the reported archive rests on one code revision. Kept unchanged; used
for no claim.
