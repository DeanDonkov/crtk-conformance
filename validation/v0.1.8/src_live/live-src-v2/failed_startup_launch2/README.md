# Start-up failure: SRC v2.0.0, campaign S, launch 2 (not pooled)

The CRTK interface script `launch_crtk_interface.py` exited at start-up (`logs/launch_crtk_interface.log`: `ERROR! Requested Joint Idx ... outside valid range [0 - -1]`, then `TypeError` in `measured_jp`), so no `/CRTK/psm1` topic existed (`logs/topics.txt`). The resting trace recorded 0 samples and the three anchor runs report `measured_cp missing` (undetermined, no probe steps). As the pre-registration provides ("A launch or run whose stack or node does not come up is repeated. It is logged and not pooled"), the records were moved here and launch 2 was repeated with `run_validation_v018.py S v2 --only-launch 2` (commit `da00219`, harness option only). The repeat is `../launch2/`; its rows are `../rows_rerun_launch2.json`. `../rows.json` is the original campaign output, including these three rows.
